@model IEnumerable<ProductListModel>

<h1>Product List</h1>


@* @section menu {
    <script type="text/javascript">
        $(function () {
            var menuItem = $('#menu-catalog');
            menuItem.addClass('active');
            var subMenuItem = menuItem.find('#menu-catalog-product').parent();
            subMenuItem.addClass('active');
        });
    </script>
}
 *@
@*  <div class="input-group">
        <a asp-controller="Product" asp-action="Create" class="btn btn-primary input-stack-margin"><i class="fa fa-plus-square"></i>&nbsp; Add new</a>
        <button type="button" id="delete" class="btn btn-danger input-stack-margin"><i class="fa fa-trash-o"></i>&nbsp; Delete (selected)</button>
    </div>

        
            <div class="col-md-12">
                <form asp-controller="Product" asp-action="Delete" method="post" id="listForm">
                    <div class="table-responsive">
                    <table id="dataTbl" class="table table-bordered table-hover">
                        <thead>
                            <tr>
                                <th></th>
                                <th>Image</th>
                                <th>Name</th>
                                <th>Stock Qty.</th>
                                <th>Published</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var product in Model)
                            {
                                <tr>
                                    <td>
                                        <input type="checkbox" id="@product.Id" name="ids" value="@product.Id" />
                                    </td>
                                    <td>
                                        @if (product.MainImage != null)
                                        {
                                            <img src="@product.MainImage" class="product-img-sm" />
                                        }
                                        else
                                        {
                                            <img src="~/images/system/no-image.png" class="product-img-sm" />
                                        }

                                    </td>
                                    <td class="sp2">
                                        @product.Name
                                    </td>
                                    <td>
                                        @product.StockQuantity
                                    </td>
                                    <td>
                                        @if (product.Published)
                                        {
                                            <i class="fa fa-check true-icon" aria-hidden="true"></i>
                                        }
                                        else
                                        {
                                            <i class="fa fa-close false-icon" aria-hidden="true"></i>
                                        }
                                    </td>
                                    <td>
                                        <div class="input-group">
                                            <a asp-controller="Product" asp-action="Edit" asp-route-id="@product.Id" id="@product.Id" class="btn btn-primary input-stack-margin">
                                                <i class="fa fa-pencil-square-o">&nbsp; Edit</i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    </div>
                </form>
            </div> <!-- col md 12 end --> *@

<p>
    <a class="btn btn-primary" asp-action="Create">Create New</a>
</p>

    <div class="col-md-12">
        <form asp-controller="Product" asp-action="Delete" method="post" id="listForm">
            <table id="dataTbl">
                <thead>
                    <tr>
                        <th><input type="checkbox" id="selectAll" /></th>
                        <th>Image</th>
                        <th>Name</th>
                        <th>Cost</th>
                        <th>Retail</th>
                        <th>Stock Qty.</th>
                        <th>Published</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var product in Model)
                    {
                        <tr>
                            <td class="sp1">
                                <input type="checkbox" id="@product.Id"name="ids" value="@product.Id" />
                            </td>
                            <td class="sp4">
                                @if (product.MainImage != null)
                                {
                                    <img src="@product.MainImage" style="max-width:200px;max-height:100px;"
                                        class="card-img-top img-fluid" />
                                }
                                else
                                {
                                    <img src="~/images/system/no-image.png" class="card-img-top img-fluid" />
                                }

                            </td>
                            <td class="sp2">
                                @product.Name
                            </td>
                            <td class="sp3">
                                @product.CostPrice
                            </td>
                            <td class="sp3">
                                @product.RetailPrice
                            </td>
                            <td class="sp3">
                                @product.StockQuantity
                            </td>
                            <td class="sp3">
                                @if (product.Published)
                                {
                                    <i class="fa fa-check true-icon" aria-hidden="true"></i>
                                }
                                else
                                {
                                    <i class="fa fa-close false-icon" aria-hidden="true"></i>
                                }
                            </td>
                            <td class="sp5">
                                <div><a asp-action="Edit" asp-route-id="@product.Id">Edit</a> </div>

                                <div><a asp-action="Details" asp-route-id="@product.Id">Details</a> </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </form> <button type="button" id="delete" class="btn btn-danger input-stack-margin">
            <i class="fa fa-trash-o"></i>&nbsp; Delete (selected)</button>
    </div> <!-- col md 12 end -->

@* 
<div class="col-lg-12 grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                  <h4 class="card-title">Hoverable Table</h4>
                  <p class="card-description">
                    Add class <code>.table-hover</code>
                  </p>
                  <div class="table-responsive">
                    <table class="table table-hover">
                      <thead>
                        <tr>
                          <th>User</th>
                          <th>Product</th>
                          <th>Sale</th>
                          <th>Status</th>
                        </tr>
                      </thead>
                      <tbody>
                        <tr>
                          <td>Jacob</td>
                          <td>Photoshop</td>
                          <td class="text-danger"> 28.76% <i class="ti-arrow-down"></i></td>
                          <td><label class="badge badge-danger">Pending</label></td>
                        </tr>
                        <tr>
                          <td>Messsy</td>
                          <td>Flash</td>
                          <td class="text-danger"> 21.06% <i class="ti-arrow-down"></i></td>
                          <td><label class="badge badge-warning">In progress</label></td>
                        </tr>
                        <tr>
                          <td>John</td>
                          <td>Premier</td>
                          <td class="text-danger"> 35.00% <i class="ti-arrow-down"></i></td>
                          <td><label class="badge badge-info">Fixed</label></td>
                        </tr>
                        <tr>
                          <td>Peter</td>
                          <td>After effects</td>
                          <td class="text-success"> 82.00% <i class="ti-arrow-up"></i></td>
                          <td><label class="badge badge-success">Completed</label></td>
                        </tr>
                        <tr>
                          <td>Dave</td>
                          <td>53275535</td>
                          <td class="text-success"> 98.05% <i class="ti-arrow-up"></i></td>
                          <td><label class="badge badge-warning">In progress</label></td>
                        </tr>
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>
            </div>
 *@

@section Scripts {
    <script>
        $('#selectAll').click(function () {
            $('input[name="ids"]').prop('checked', this.checked);
        });
        // delete
        $('#delete').on('click', function (event) {
            var ids = [];
            var form = $('#listForm');
            var checkboxArray = document.getElementsByName('ids');

            // check if checkbox is checked
            for (var i = 0; i < checkboxArray.length; i++) {
                if (checkboxArray[i].checked)
                    ids.push(checkboxArray[i].value);
            }

            // submit form
            if (ids.length > 0) {
                if (confirm("Are you sure you want to delete this item(s)?")) {
                    form.submit();
                }
            }
        });
    </script>
    @* <script>
        // onready
        $(function () {
            initializeDataTable();
            initializeICheck();
        });

        // delete
        $('#delete').on('click', function (event) {
            var ids = [];
            var form = $('#listForm');
            var checkboxArray = document.getElementsByName('ids');

            // check if checkbox is checked
            for (var i = 0; i < checkboxArray.length; i++) {
                if (checkboxArray[i].checked)
                    ids.push(checkboxArray[i].value);
            }

            // submit form
            if (ids.length > 0) {
                if (confirm("Are you sure you want to delete this item(s)?")) {
                    form.submit();
                }
            }
        });

        // datatable highlight selected
        $('#dataTbl tbody').on('click', 'tr', function (event) {
            var nodeName = event.target.nodeName;

            if (nodeName != "BUTTON" &&
                nodeName != "A" &&
                nodeName != "IMG" &&
                nodeName != "I") {
                $(this).toggleClass('selected');
                var id = $(this).closest('tr').find('input').attr('id');
                $('#' + id).iCheck('toggle');
            }
        });

        // icheck highlight selected
        $('input').on('ifChecked', function (event) {
            var row = $(this).closest('tr');
            if (!row.hasClass('selected'))
                row.toggleClass('selected');
        });

        $('input').on('ifUnchecked', function (event) {
            var row = $(this).closest('tr');
            if (row.hasClass('selected'))
                row.toggleClass('selected');
        });

        // initialize DataTable
        function initializeDataTable() {
            $('#dataTbl').DataTable({
                paging: true,
                lengthChange: false,
                searching: false,
                ordering: true,
                info: true,
                autoWidth: false,
                order: [2, 'asc'],
                columnDefs: [
                    { "width": "5%", "targets": 0, "orderable": false },
                    { "targets": 1, "orderable": false },
                    { "targets": 2, "orderable": true },
                    { "targets": 3, "orderable": true },
                    { "targets": 4, "orderable": false },
                    { "targets": 5, "orderable": false }
                ]
            });
        };

        // initialize ICheck
        function initializeICheck() {
            // iCheck
            $('input').iCheck({
                checkboxClass: 'icheckbox_square-blue',
                radioClass: 'iradio_square-blue',
                increaseArea: '20%' // optional
            });
        };
    </script> *@
}

